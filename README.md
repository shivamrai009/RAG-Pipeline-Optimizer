# RAG-Pipeline-Optimizer
Every company has a RAG (Retrieval-Augmented Generation) system. Almost no company knows if their RAG is good. Is chunk_size=512 better than 1024? Is Cohere-v3 a better embedder than BERT for their data? They're just guessing.
